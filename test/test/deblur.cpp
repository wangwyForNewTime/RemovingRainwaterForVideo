#include "stdafx.h"
#include"basicoperation.h"
#include"restore.h"
#include"assessment.h"
#include"estimatepar.h"
/*
int main(int argc, char** argv)
{
	Mat srcImage;
	srcImage = imread("D:\\STL\\Camera\\test_image\\Motion_blur\\fishes.jpg", 0);
	imageInit(srcImage, srcImage);
	namedWindow("ԭͼ", WINDOW_AUTOSIZE);
	imshow("ԭͼ", srcImage);

	fft2(srcImage, srcImage);
	fftshift(srcImage, srcImage);
	fftShow(srcImage);

	waitKey(0);

	return(0);
}
*/